factorial = 1;
iterator = 0;

iterator = iterator + 1;
factorial = factorial * iterator;
factorial.print();

iterator = iterator + 1;
factorial = factorial * iterator;
factorial.print();

iterator = iterator + 1;
factorial = factorial * iterator;
factorial.print();

iterator = iterator + 1;
factorial = factorial * iterator;
factorial.print();

iterator = iterator + 1;
factorial = factorial * iterator;
factorial.print();

